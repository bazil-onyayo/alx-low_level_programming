#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @arg: The integer to be checked
 *
 * Return: Always (0) Success
 */
int _abs(int)
{
	if (n >= 0)
	{
		_putchar(n);
	} else if (n < 0)
	{
		_putchar(n * -1);
	}

	return (0);
}
