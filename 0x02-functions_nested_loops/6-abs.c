#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @arg: The integer to be checked
 *
 * Return: Always (0) Success
 */
int _abs(int)
{
	if (int >= 0)
	{
		_putchar(int);
	} else if (int < 0)
	{
		_putchar(int * -1);
	}

	return (0);
}
